<div class="modal fade" id="input-modal-transaction">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h4 class="modal-title">Create Transaction</h4>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <form class="form-horizontal" method="POST" action="/reservation/confirm">
            <div class="card-body">
              <div class="form-group row">
                <!-- Hidden _id parsing -->
                  <input type="text" class="form-control _id-parsing" name="id" required hidden>
                <!-- ./Hidden _id parsing -->
                <label for="member" class="col-sm-2 col-form-label">Member</label>
                <div class="col-sm-10">
                  <select class="form-control" name="member">
                    <option selected disabled>Select member</option>
                    <% for(let i = 0; i < member.length; i++) { %>
                      <option value="<%= member[i].username %>">
                        <%= member[i].username %>
                      </option>
                   <% } %>
                  </select>
                </div>
              </div>
              <div class="form-group row">
                <div class="col-sm-2">
                  <label>Service</label>
                </div>
                <div class="col-sm-10">
                  <select class="form-control" name="serviceId">
                    <option selected disabled>Select Service</option>
                    <% for(let i = 0; i < service.length; i++) { %>
                      <option value="<%= service[i]._id %>">
                        <%= service[i].name %>  |  <%= service[i].price %>
                      </option>
                    <% } %>
                  </select>
                </div>
              </div>
              <div class="form-group row">
                <div class="col-sm-2">
                  <label>payment</label>
                </div>
                <div class="col-sm-10">
                  <select class="form-control" name="payment">
                    <option selected disabled>Select Payment</option>
                    <option value="Cash">Cash</option>
                    <option value="BCA">BCA</option>
                  </select>
                </div>
              </div>
            </div>
            <div class="modal-footer justify-content-between">
              <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
              <button type="submit" class="btn btn-primary">Create</button>
            </div>
          </form>
        </div>
      </div>
      <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
  </div>